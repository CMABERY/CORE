# CPO in CORE

This directory hosts CPO integration assets.

- `kit/` is the **vendored, frozen** CPO Starter Kit (canon). Treat as read-only.
- Future runtime implementation (commit service) will live under `runtime/` (not part of PR 1).

Canon: `kit/VERSION` pins the frozen release + upstream artifact hash.
