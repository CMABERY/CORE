# CORE

This repository currently contains:

- `codex_like_mvp_project/` — a minimal **transaction-first** coding agent kernel (stdlib-only)

## Quickstart

```bash
cd codex_like_mvp_project
python -m codex_like_mvp.client.mvp_cli demo
```

## Hygiene
- UTF-8 + LF enforced via `.editorconfig` / `.gitattributes`
- CI runs self-tests and validates the most recent demo transaction

